namespace Amazon.RA2
{
    internal class Module
    {
        public int findMaximumSustainableClusterSize(List<int> processingPower, List<int> bootingPower, long powerMax)
        {
            int maxClusterSize = 0;
            int firstClusterIndex = 0;
            int lastClusterIndex = 0;

            while (firstClusterIndex < processingPower.Count && lastClusterIndex < processingPower.Count)
            {

            }

            return maxClusterSize;
        }













    }
}
